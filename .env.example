# Stat Tools 环境变量配置示例
# 复制此文件为 .env.local 并按需填入实际值

# ===================
# Next.js 公共变量（会暴露到浏览器）
# ===================

# Google Analytics（可选）
NEXT_PUBLIC_GA_MEASUREMENT_ID=G-XXXXXXXXXX

# ===================
# 应用运行配置
# ===================

# 生产环境数据库文件路径（容器/服务器上使用）
# 不设置时默认 /app/data/statcal.db（仅生产）
DATABASE_PATH=/app/data/statcal.db

# 站点 URL（用于 sitemap 等）
SITE_URL=https://localhost:3000

# 搜索配置（FTS 开关）
# 设置为 1 启用 FTS；未设置或为 0 时回退到 LIKE 搜索
USE_FTS_SEARCH=0
# 或者设置模式为 fts 启用全文搜索
CONTENT_SEARCH_MODE=like

# ===================
# 开发/测试工具变量（可选）
# ===================

# 打包大小预算检查（KB）
JS_BUDGET_KB=90
CHUNK_BUDGET_KB=30

# 导出脚本默认数据库文件路径
DATABASE_FILE=./data/statcal.db

# 测试基址
BASE_URL=http://localhost:3000

# 测试日志更详细
VERBOSE_TESTS=
