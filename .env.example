# Stat Tools 环境变量配置示例
# 复制此文件为 .env.local 并填入实际值

# ===================
# Next.js 环境变量
# ===================

# 公开环境变量 (前缀必须为 NEXT_PUBLIC_)
NEXT_PUBLIC_APP_NAME=Stat Tools
NEXT_PUBLIC_APP_VERSION=1.0.0
NEXT_PUBLIC_ENVIRONMENT=development

# Google Analytics (可选)
NEXT_PUBLIC_GA_MEASUREMENT_ID=G-XXXXXXXXXX

# ===================
# Cloudflare 配置
# ===================

# Cloudflare Account ID (用于部署)
CF_ACCOUNT_ID=your-cloudflare-account-id

# Cloudflare API Token (用于 Workers 部署)
CF_API_TOKEN=your-cloudflare-api-token

# Cloudflare Pages Token (用于 Pages 部署)
CF_PAGES_TOKEN=your-cloudflare-pages-token

# Cloudflare Pages 项目名称
CF_PAGES_PROJECT=stat-tools

# ===================
# Worker 环境变量
# ===================

# Worker 签名密钥 (生产环境应通过 wrangler secret put 设置)
SIGN_SECRET=your-very-secure-secret-key-min-32-chars

# R2 存储桶名称
R2_BUCKET_NAME=stat-tools-exports

# ===================
# 开发环境配置
# ===================

# 开发服务器端口
PORT=3000

# API 基础 URL
API_BASE_URL=http://localhost:3000

# Worker 开发 URL
WORKER_DEV_URL=http://localhost:8787

# ===================
# 测试环境变量
# ===================

# E2E 测试基础 URL
BASE_URL=http://localhost:3000

# 测试用密钥
TEST_SIGN_SECRET=test-secret-for-unit-tests

# ===================
# 日志配置
# ===================

# 日志级别 (debug, info, warn, error)
LOG_LEVEL=info

# ===================
# 安全配置
# ===================

# JWT 密钥 (如果使用)
JWT_SECRET=your-jwt-secret-key

# CORS 允许的域名
ALLOWED_ORIGINS=http://localhost:3000,https://your-domain.com